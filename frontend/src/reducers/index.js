import { combineReducers } from "redux";
import posts from "./posts";

export default combineReducers({    //we can add all the individual reducers here
    posts,  // in this case we have only posts 
})
